import SwiftUI
import MurmurCore

// EntryEditSheet has been consolidated into EntryDetailView.
// Editing (summary, category, priority, notes) is now done inline
// in the detail view — no separate sheet is needed.
